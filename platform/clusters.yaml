# Platform cluster configuration
# Each team defines their clusters per environment here.
# Add/remove entries and run `terraform apply` to reconcile.

repo_url: https://github.com/rui-armada/infra.git

teams:
  - name: bestversion
    environments:
      - name: prod

  # Example: add another team
  # - name: payments
  #   environments:
  #     - name: dev
  #     - name: staging
  #     - name: prod
