# Platform cluster configuration
# Each team defines their clusters, repo, and chart path here.
# Add/remove entries and run `terraform apply` to reconcile.

platform_repo: https://github.com/rui-armada/infra.git

teams:
  - name: bestversion
    repo: https://github.com/rui-armada/bestversion.git
    chart_path: charts/bestversion
    branch: main
    environments:
      - name: prod

  # Example: add another team
  # - name: payments
  #   repo: https://github.com/rui-armada/payments.git
  #   chart_path: charts/payments
  #   branch: main
  #   environments:
  #     - name: dev
  #     - name: staging
  #     - name: prod
